# Natural-Language-Processing-Tasks
NLP spam detection project using SMS dataset. Includes preprocessing (tokenization, stopwords, lemmatization), feature extraction (BoW, TF-IDF, bigrams), and Naive Bayes model with performance comparison.
